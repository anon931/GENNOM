spring:
  profiles:
    active: dev
  application:
    name: gennom-backend

server:
  port: 8080
  error:
    include-message: always

logging:
  level:
    com.gennom: DEBUG
    org.springframework.web: INFO